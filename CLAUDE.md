# Claude Code guide

@AGENTS.md

Project skills live in `.agents/skills/` (managed by `npx skills`): `nestjs-patterns`, `design-system`, `grilling` (planning interviews), and `grill-me` (alias that runs a grilling session).
